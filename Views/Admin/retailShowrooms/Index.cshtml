@model IEnumerable<Nexus_Service_Marketing_System.Models.retailShowrooms>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h1>Index</h1>

<h4>Retail Showrooms</h4>

<hr />

<p>
    <a asp-controller="RetailShowrooms" asp-action="Create"><i class="material-icons" style="padding-right: 10px">playlist_add</i>Create New</a>
</p>

<hr />

@using (Html.BeginForm())
{
    <input type="text" name="txtRetailShowroomName" placeholder="Enter retail showroom name..." />
    <input type="submit" name="btnSearch" value="Search" class="btn-outline-info" />
}

<br />

<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RetailShowroom_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RetailShowroom_city_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RetailShowroom_status)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RetailShowroom_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RetailShowroom_city_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RetailShowroom_status)
                </td>
                <td>
                    <a asp-controller="RetailShowrooms" asp-action="Details" asp-route-id="@item.RetailShowroom_id"><button class="btn-outline-info">Details</button></a>
                    <a asp-controller="RetailShowrooms" asp-action="Update" asp-route-id="@item.RetailShowroom_id"><button class="btn-outline-primary">Edit</button></a>
                </td>
            </tr>
        }
    </tbody>
</table>

<br />

<div class="d-flex justify-content-center">
    <h3 class="text-light">@ViewBag.Msg</h3>
</div>