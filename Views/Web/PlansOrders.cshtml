@model IEnumerable<Nexus_Service_Marketing_System.Models.plans_orders>

@{
    ViewData["Title"] = "Plans Orders";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main id="main">

    <!-- ======= Our Plans Orders Section ======= -->
    <section class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>Plans Orders</h2>
                <ol>
                    <li><a asp-controller="Web" asp-action="Index">Home</a></li>
                    <li><a asp-controller="Web" asp-action="Services">Services</a></li>
                    <li>Plans Orders</li>
                </ol>
            </div>

        </div>
    </section><!-- End Our Plans Orders Section -->

    <!-- ======= Plans Orders Section ======= -->
    <section class="orders">
        <div class="container">
            <div class="d-flex justify-content-center">
                @using (Html.BeginForm())
                {
                    <input type="text" name="txtPlansOrdersId" placeholder="Enter plans orders id..." />
                    <input type="submit" name="btnSearch" value="Search" class="btn-outline-info" />
                }
            </div>

            <br />

            <div class="d-flex justify-content-center">
                <h3 class="text-primary">@ViewBag.Msg</h3>
            </div>

            <br />

            @if (Model != null)
            {
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Plans_orders_id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Plans_orders_connection_name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Plans_orders_order_id)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Plans_orders_plan_price)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Plans_orders_status)
                            </th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Plans_orders_id)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Plans_orders_connection_name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Plans_orders_order_id)
                                </td>
                                <td>
                                    $@Html.DisplayFor(modelItem => item.Plans_orders_plan_price)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Plans_orders_status)
                                </td>
                                <td>
                                    <a asp-controller="Web" asp-action="PlansOrdersDetails" asp-route-id="@item.Plans_orders_id"><button class="btn-info">Details</button></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </section><!-- End Plans Orders Section -->

</main><!-- End #main -->